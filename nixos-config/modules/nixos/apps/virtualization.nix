# Virtualization — VirtualBox / libvirtd (disabled by default)
{ config, lib, pkgs, ... }:

{
  # Uncomment to enable:
  # virtualisation.virtualbox.host.enable = true;
  # virtualisation.libvirtd.enable = true;
}
